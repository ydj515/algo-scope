# Coding Style & Naming Conventions

- 언어: TypeScript + React 함수형 컴포넌트
- 들여쓰기: 스페이스 2칸
- 파일명: 라우트 파일은 Next.js 관례(`page.tsx`, `layout.tsx`) 유지
- 식별자: 컴포넌트는 `PascalCase`, 변수/함수는 `camelCase`
- 스타일: 전역은 `app/globals.css`, 컴포넌트 범위 스타일을 우선

포맷팅/정적 분석 규칙은 `eslint-config-next`를 기준으로 합니다.
